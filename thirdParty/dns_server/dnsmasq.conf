# Ignora o /etc/hosts interno do container
no-hosts

# Aponta para o arquivo de hosts externo que vamos mapear via volume
addn-hosts=/etc/dnsmasq.hosts

# Servidores DNS para páginas da internet
server=1.1.1.1
server=1.0.0.1

cache-size=1000